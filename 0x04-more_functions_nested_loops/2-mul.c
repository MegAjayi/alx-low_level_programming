#include "main.h"
/**
 * mul - function in the programe
 * Description: to multiply two different int
 * @a: paremeter one
 * @b: paremeter two
 * Return: result an int
 */
int mul(int a, int b)
{
	return (a * b);
}
